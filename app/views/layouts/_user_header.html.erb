<ul class="nav nav-pills pull-right">
	<% if user_signed_in? %>
    <div id = "user_sign_status"><%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger' %><div/>
  <% else %>
  <div id = "user_sign_status">
    <%= link_to "Sign in", new_user_session_path, :class => "btn btn-success" %> <%= link_to "Sign up", new_user_registration_path, :class => "btn btn-success" %></div>
  <% end %>
</ul>